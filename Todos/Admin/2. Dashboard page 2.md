# 1. Dashboard page

## Reference Files
- ./src/components/Admin/IndexPage.vue

## Task

- [ ] Update the dashboard page to modify the 3 widgets: Today's Meeting, Today's Task, and Upcoming Event Reminders,
To fetch all projects, right now i statically use 1 projectId -> this part on `getEvents()` function:
    ```const events = this.$fbref(
        this.$fbdb,
        `events/${'5c42d201-49e0-4424-aa8a-4c889ae1b3db'}`
      )```
- [] Use the same code structure and pattern in place.

For this task, focus only on the 3 widgets mentioned above.